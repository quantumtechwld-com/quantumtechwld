# Handoff — 2026-02-21

## Status geral
Projeto da agência criado e funcionando localmente com Next.js + TypeScript + Tailwind.

## O que foi concluído
- Landing page premium implementada.
- Formulário de lead implementado.
- Endpoint local `POST /api/lead` implementado.
- Integração com webhook n8n configurável por `N8N_WEBHOOK_URL`.
- Página de confirmação `/obrigado` implementada.
- Workflow n8n de captura criado para importação.

## Arquivos principais
- `src/app/page.tsx`
- `src/components/lead-form.tsx`
- `src/app/api/lead/route.ts`
- `src/app/obrigado/page.tsx`
- `.env.local`
- `n8n/workflow-lead-capture.json`
- `n8n/README.md`

## Ambiente atual
- URL do site local: `http://localhost:3000`
- Webhook configurado: `https://agencyfirst.app.n8n.cloud/webhook/lead-capture`
- Build local: OK

## Problema pendente
O webhook do n8n está respondendo, porém retorna validação de campos incompletos (`400`).
Resposta observada:

```json
{"ok":false,"error":"Dados do lead incompletos.","requiredFields":"name,email,service,budget,message"}
```

## Diagnóstico
- App local está online (porta 3000 ativa).
- Endpoint `/api/lead` está chamando o n8n.
- Falha atual é de mapeamento/validação no workflow n8n (nó IF), não de conectividade.

## Ajustes já feitos no código
No backend local (`src/app/api/lead/route.ts`) o payload foi ampliado para enviar:
- campos no nível raiz (`name`, `email`, etc.), e
- também `body` com os mesmos dados.

## Plano para amanhã (passo a passo)
1. Abrir workflow no n8n.
2. No nó **Validar Campos (IF)**, ajustar expressões para:
   - `{{$json.body.name}}`
   - `{{$json.body.email}}`
   - `{{$json.body.service}}`
   - `{{$json.body.budget}}`
   - `{{$json.body.message}}`
3. Operador de cada condição: **is not empty**.
4. Salvar workflow e deixar **Active = ON**.
5. Enviar formulário no site e validar execução com HTTP 200 no n8n.

## Observação
Também existe workflow corrigido no repositório (`n8n/workflow-lead-capture.json`) que pode ser reimportado para evitar ajuste manual.
